<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/ic_launcher_background" />
    <!-- The art is a full-bleed purple square + centred cat; inset it into the
         adaptive safe zone so the launcher mask never clips the cat, and its
         purple edges blend seamlessly into the matching background color. -->
    <foreground>
        <inset
            android:drawable="@mipmap/ic_launcher_foreground"
            android:inset="16%" />
    </foreground>
</adaptive-icon>
